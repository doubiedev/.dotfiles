# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

#-------------------------------------------
# TPM
#-------------------------------------------

set -g @plugin 'rose-pine/tmux'
set -g @rose_pine_variant 'moon'
set -g @rose_pine_host 'on'
set -g @rose_pine_status_left_append_section ' '
set -g @rose_pine_window_status_separator ' '

set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-restore-color-schemes 'off'

#-------------------------------------------
# Keybinds
#-------------------------------------------

# List sessions
bind s choose-tree -sZ -O name
bind w choose-tree -sZ -O time

# Window switching
bind -rn M-w last-window

# Alt+(1-10) window selection
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

# Alt+(hjkl) pane selection
bind -n C-M-h select-pane -L
bind -n C-M-j select-pane -D
bind -n C-M-k select-pane -U
bind -n C-M-l select-pane -R

# Switch to notes session (back and forth)
bind-key -n M-` run-shell '
  CURRENT=$(tmux display-message -p "#S");
  if [ "$CURRENT" = "notes" ]; then
    tmux switch-client -l;
  else
    tmux switch-client -t notes;
  fi'

#-------------------------------------------
# Options
#-------------------------------------------

set -g mouse on

#-------------------------------------------
# ThePrimeagen's Keybinds
#-------------------------------------------
set -s escape-time 0

unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

bind r source-file ~/.tmux.conf
set -g base-index 1

set-window-option -g mode-keys vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# vim-like pane switching
# bind -r ^ last-window
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

bind -r D neww -c "#{pane_current_path}" "[[ -e TODO.md ]] && nvim TODO.md || nvim ~/notes/TODO.md"
bind -r O neww -c "#{pane_current_path}" "nvim ~/notes"

# forget the find window.  That is for chumps
bind-key -r f run-shell "tmux neww ~/.local/bin/tmux-sessionizer"
bind-key -r i run-shell "tmux neww tmux-cht.sh"

bind-key -r C-h run-shell "~/.local/bin/tmux-sessionizer ~/.dotfiles"
bind-key -r C-j run-shell "~/.local/bin/tmux-sessionizer ~/"
bind-key -r C-k run-shell "~/.local/bin/tmux-sessionizer ~/notes"
bind-key -r C-l run-shell "~/.local/bin/tmux-sessionizer ~/projects/tutorials"

# set -g default-terminal "tmux-256color"
# set-option -ga terminal-overrides ",xterm*:Tc"

# Installs Plugins on new machines
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'
